DATABASE_URL=postgresql+psycopg://ari:ari@localhost:5433/ari
REDIS_URL=redis://localhost:6379/0

# Required to run the demo pipeline against the real Claude API (app/llm.py).
# Not required for `--mock` mode or the test suite.
ANTHROPIC_API_KEY=

# Anomaly detection thresholds (Day 6) — all optional, shown here with
# their defaults from app/config.py. Uncomment to override.
# LOOP_THRESHOLD=3
# COST_HARD_THRESHOLD_TOKENS=5000
# COST_ZSCORE_THRESHOLD=2.0
# COST_ZSCORE_MIN_SAMPLES=3
